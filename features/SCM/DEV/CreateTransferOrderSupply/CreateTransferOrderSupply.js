import { Given, When, Then } from "cypress-cucumber-preprocessor/steps";
 
Given('Given the API end point for Create Transfer Order API', () => {
	cy.Pending();
});

When('user update the payload with {string},{string},{string},{string},{string}', (args1) => {
	cy.Pending();
});

Given('Given User send the post request to the endpoint', () => {
	cy.Pending();
});

Then('User should have {int} response code successful and capture interface number', (args1) => {
	cy.Pending();
});

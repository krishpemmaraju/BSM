# .github/workflows/test-runner.yml
name: Test Self-Hosted Runner

on:
  workflow_dispatch  # Manual trigger for testing

jobs:
  test-connectivity:
    runs-on: self-hosted  # Uses your runner
    
    steps:
      - name: Check Runner Environment
        run: |
          echo "Runner is working!"
          hostname
          whoami
      - name: Test Jenkins Connectivity
        shell: powershell 
        run: |
          echo "🔍 Testing Jenkins connectivity..."
          
          # Method 1: Using curl.exe (actual curl)
          try {
              echo "Testing with curl.exe..."
              $curlResult = curl.exe -I --connect-timeout 10 --max-time 30 http://svttstif001:9092 2>&1
              if ($LASTEXITCODE -eq 0) {
                  echo "[OK] curl.exe: Jenkins is reachable"
                  echo $curlResult
              } else {
                  echo "[ERROR]: curl.exe: Cannot reach Jenkins"
                  echo $curlResult
              }
          } catch {
              echo "[ERROR]: curl.exe error: $($_.Exception.Message)"
          }   

          

